import { expect } from "chai";
import { Given, When, Then } from "cucumber";

Given('a passing step', function (table) {
  return 'pending';
});

When('I run yachr agaist it', function () {
  return 'pending';
});

Then('a summary showing one passing feature and one passing scenario', function () {
  return 'pending';
});
